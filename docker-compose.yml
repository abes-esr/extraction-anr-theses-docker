services:
  extraction-anr-theses:
    container_name: extraction-anr-theses
    build: .
    volumes:
      - "${DATA_PATH}:/starstock"
      - "${OUTPUT_PATH}:/output"
    env_file:
      - .env
    environment:
      - MAX_FILES=${MAX_FILES}
      - OFFSET=${OFFSET}
